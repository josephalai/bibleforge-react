# ── Server configuration (local dev) ─────────────────────────────────────────
# For Docker, ports come from the root .env — see /.env.example
# Copy this to server/.env for local development (without Docker)

PORT=3001

DB_HOST=localhost
DB_PORT=3306
DB_USER=bibleforge
DB_PASSWORD=bibleforge
DB_NAME=bf
